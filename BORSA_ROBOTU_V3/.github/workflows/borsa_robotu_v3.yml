name: Borsa V3 Otomatik Tarama

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  run_analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📂 Proje Kodlarını Çek
        uses: actions/checkout@v4

      - name: 🐍 Python Ortamını Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 🛠️ Kütüphaneleri Yükle (Hata Giderilmiş Versiyon)
        run: |
          python -m pip install --upgrade pip
          pip install pandas yfinance requests numpy
          # pandas-ta'yı en güvenli ve kararlı versiyonuyla kuruyoruz
          pip install pandas-ta==0.3.14b0

      - name: 🤖 Robotu Ateşle
        run: python BORSA_ROBOTU_V3/ana_kontrol.py
        env:
          INSTANCE_ID: ${{ secrets.INSTANCE_ID }}
          TOKEN: ${{ secrets.TOKEN }}
          TELEFON: ${{ secrets.TELEFON }}
name: Borsa V3 Stratejik Analiz

on:
  schedule:
    # Türkiye saatine (UTC+3) göre mühürlenmiş 5 kritik seans vakti
    - cron: '30 06 * * 1-5'  # 09:30 TSİ (Açılış Öncesi Son Kontrol)
    - cron: '15 07 * * 1-5'  # 10:15 TSİ (Açılış İlk Analiz)
    - cron: '00 10 * * 1-5'  # 13:00 TSİ (Öğle Arası Değerlendirme)
    - cron: '30 14 * * 1-5'  # 17:30 TSİ (Kapanış Öncesi Fırsatlar)
    - cron: '20 15 * * 1-5'  # 18:20 TSİ (Gün Sonu Kesin Onay)
  workflow_dispatch:         # İstediğin an telefonundan manuel tetikleme sağlar

jobs:
  run_analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 60      # Analiz kapsamlı olduğu için süre geniş tutuldu

    steps:
      - name: 📂 Proje Kodlarını Çek
        uses: actions/checkout@v4

      - name: 🐍 Python Ortamını Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 🛠️ Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install pandas yfinance requests numpy pandas-ta

      - name: 🤖 Robotu Ateşle
        # DİKKAT: Ana dizindeki 'borsa_robotu_yeni.py' dosyasını çalıştırır
        run: python borsa_robotu_yeni.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

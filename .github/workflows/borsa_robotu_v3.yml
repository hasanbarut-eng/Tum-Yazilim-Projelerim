name: Borsa V3 Otomatik Tarama

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  run_analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📂 Proje Kodlarını Çek
        uses: actions/checkout@v4

      - name: 🐍 Python Ortamını Kur (Güncel Sürüm)
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' # Sürümü 3.12'ye yükselttik

      - name: 🛠️ Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install pandas yfinance requests numpy
          # En sorunsuz kurulum yöntemi:
          pip install pandas-ta

      - name: 🤖 Robotu Ateşle
        run: python BORSA_ROBOTU_V3/ana_kontrol.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}


name: Borsa Stratejik Analiz Otomasyonu

on:
  schedule:
    # Türkiye saatine (UTC+3) göre ayarlanmış 5 seans vakti:
    - cron: '30 06 * * 1-5'  # 09:30 TSİ
    - cron: '15 07 * * 1-5'  # 10:15 TSİ
    - cron: '00 10 * * 1-5'  # 13:00 TSİ
    - cron: '30 14 * * 1-5'  # 17:30 TSİ
    - cron: '20 15 * * 1-5'  # 18:20 TSİ
  workflow_dispatch:         # Manuel çalıştırma butonu

jobs:
  run_analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 60      # Kapsamlı tarama için süre sınırı

    steps:
      - name: 📂 Proje Kodlarını Çek
        uses: actions/checkout@v4

      - name: 🐍 Python Ortamını Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 🛠️ Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; else pip install pandas yfinance requests numpy pandas-ta; fi

      - name: 🤖 Robotu Ateşle
        run: python ana_kontrol.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

name: Borsa V3 Stratejik Analiz

on:
  schedule:
    # 🛑 SADECE PAZARTESİ - CUMA (Hafta sonu ateşleme yok!)
    # Dakika | Saat (UTC) | Gün | Ay | Haftanın Günü (1=Pzt, 5=Cum)
    
    - cron: '30 06 * * 1-5'  # 🇹🇷 TR Saati: 09:30 (Açılış Sinyalleri)
    - cron: '15 07 * * 1-5'  # 🇹🇷 TR Saati: 10:15 (Trend Teyidi)
    - cron: '00 10 * * 1-5'  # 🇹🇷 TR Saati: 13:00 (Öğle Arası)
    - cron: '40 14 * * 1-5'  # 🇹🇷 TR Saati: 17:40 (Kapanış Öncesi)
    - cron: '15 15 * * 1-5'  # 🇹🇷 TR Saati: 18:15 (Gün Sonu Özeti)

  workflow_dispatch:        # İstediğinde manuel ateşlemek için bu da kalsın.

jobs:
  run_analysis:
    runs-on: ubuntu-latest
    # SÜRE SINIRI 60 DAKİKAYA ÇIKARILDI (15 dakika yetmiyordu)
    timeout-minutes: 60 

    steps:
      - name: 📂 Proje Kodlarını Çek
        uses: actions/checkout@v4

      - name: 🐍 Python Ortamını Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 🛠️ Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install pandas yfinance requests numpy pandas-ta html5lib lxml

      - name: 🤖 Robotu Ateşle
        # BURASI ÖNEMLİ: ana_kontrol.py içinde döngü (while True) olmamalı!
        run: python BORSA_ROBOTU_V3/ana_kontrol.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
